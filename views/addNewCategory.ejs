<%- include('header') %>
    <div class="container">
        <div class="row">
            <div id="signupbox" style="margin-top:50px" class="mainbox col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
              <div class="panel panel-info">
                  <div class="panel-heading">
                      <div class="panel-title">Add New Password Category</div>
                  </div>
                  <div class="panel-body" >
                  <form action="/add-new-category" id="EmployeeForm" class="form-horizontal" method="post"  role="form">
                    <div class="form-group">
                      <% if(errors!='') {%>
                          <p style="text-align:center; color:red;"><%= errors.passcad.msg %></p>
                        <% } %>
                        <% if(success!='') { %>
                          <p style="text-align:center; color:green;"><%= success %></p>
                        <% } %>
                      <label for="uname" class="col-md-3 control-label">New Category</label>
                        <div class="col-md-9">
                          <input type="text" class="form-control" name="passcad" placeholder="Enter Your New Category">
                        </div>
                    </div>
                    <div class="form-group">
                      <div class="col-md-offset-3 col-md-9">
                        <input type="submit" name="submit" value="Add Category" class="btn btn-primary">
                      </div>
                    </div>
                    <p><a href="/passwordCategory">View all category</a></p>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
      <%- include('footer') %>